import React, { Component } from 'react';
import classnames from 'classnames';
import { Tabs, Tab } from 'Shared/Tabs';

import './styles.less';
export default function HouseFurniture(props) {
    const { furnitrue } = this.props;
}
